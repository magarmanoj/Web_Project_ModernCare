import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.moderncare',
  appName: 'moderncare',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
